{
  "name": "jana-bikas-project",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix backend && cd frontend && npm install --include=dev",
    "build": "npm run install-all && cd frontend && npx vite build",
    "start": "npm start --prefix backend"
  }
}
